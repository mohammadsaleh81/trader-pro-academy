
/**
 * Safely converts an ID to a string
 * @param id - The ID to convert, which could be a string or number
 * @returns The ID as a string
 */
export const idToString = (id: string | number): string => {
  return String(id);
};
